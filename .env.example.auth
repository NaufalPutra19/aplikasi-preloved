# .env Configuration Example for Authentication Setup
# Copy required lines ke file .env Anda dan sesuaikan dengan data real

# ============================================================
# GOOGLE OAUTH - REQUIRED untuk Login/Register dengan Google
# ============================================================
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# ============================================================
# EMAIL CONFIGURATION - REQUIRED untuk Email Verification
# ============================================================

# OPTION 1: Mailtrap (Recommended untuk Development)
MAIL_MAILER=smtp
MAIL_HOST=live.smtp.mailtrap.io
MAIL_PORT=587
MAIL_USERNAME=your_mailtrap_username
MAIL_PASSWORD=your_mailtrap_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@yourapp.com
MAIL_FROM_NAME="The Order"

# OPTION 2: Gmail (uncomment ini dan comment OPTION 1)
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your_email@gmail.com
# MAIL_PASSWORD=your_app_password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=your_email@gmail.com
# MAIL_FROM_NAME="The Order"

# OPTION 3: Mailgun (uncomment ini dan comment OPTION 1)
# MAIL_MAILER=mailgun
# MAILGUN_DOMAIN=your_domain.mailgun.org
# MAILGUN_SECRET=your_mailgun_secret
# MAIL_FROM_ADDRESS=noreply@your_domain.com
# MAIL_FROM_NAME="The Order"

# ============================================================
# Catatan:
# ============================================================
# 1. GOOGLE_CLIENT_ID dan GOOGLE_CLIENT_SECRET didapat dari:
#    https://console.cloud.google.com
#
# 2. Untuk Gmail, gunakan App Password bukan password biasa:
#    https://support.google.com/accounts/answer/185833
#
# 3. Untuk Mailtrap:
#    - Daftar di https://mailtrap.io
#    - Ambil SMTP credentials dari dashboard
#
# 4. GOOGLE_REDIRECT_URI harus sesuai dengan yang didaftarkan
#    di Google Cloud Console
#
# 5. MAIL_FROM_ADDRESS akan ditampilkan sebagai sender di email
#
# ============================================================
